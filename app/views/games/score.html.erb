
<div class="container">
  <h1>Result</h1>

  <% if @ingrid && @english %>
    <p><%= "Conglatulations! #{@word} is a valid English word!" %></p>
  <% elsif @english && @ingrid = false %>
    <p><%= "Sorry but #{@word} can't be built our of #{@currentgrid}" %></p>
  <% else %>
    <p><%= "Sorry but #{@word} does not seem to be a valid English word..." %></p>
  <% end %>

  <%= link_to 'New Game', new_path %>
</div>
